# Claude 对话会话: 6b090074-731b-480b-aba7-1728661d9d7c

## 🧑 用户 (2026-06-04T10:06:27)

# 1. 写入环境变量到 ~/.zshrc
cat >> ~/.zshrc << 'EOF'

# Claude Code API 配置（中转站：uni-api.cstcloud.cn）
export ANTHROPIC_BASE_URL="https://uni-api.cstcloud.cn/v1"
export ANTHROPIC_AUTH_TOKEN="9378cea3fc553d83c6f47260f6c70fcec1718f6b8850940f4520a8d1da2b2a27"
export ANTHROPIC_MODEL="claude-3-5-sonnet-20241022"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="claude-3-5-haiku-20251022"
export CLAUDE_CODE_SUBAGENT_MODEL="claude-3-5-sonnet-20241022"
EOF

# 2. 重新加载配置
source ~/.zshrc

# 3. 测试 API 是否可用（这一步会返回结果，请检查）
echo ">>> 测试 API 连接..."
curl -s -X POST "https://uni-api.cstcloud.cn/v1/chat/completions" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 9378cea3fc553d83c6f47260f6c70fcec1718f6b8850940f4520a8d1da2b2a27" \
  -d '{
    "model": "claude-3-5-sonnet-20241022",
    "messages": [{"role": "user", "content": "Say hello"}],
    "max_tokens": 20
  }' | jq -r '.choices[0].message.content // .error.message // "连接成功但返回格式异常"'

echo ""
echo ">>> 环境变量已配置，测试完成。现在可以启动 Claude Code。"

# 4. 启动 Claude Code
claude

---

## 🤖 Claude (2026-06-04T10:06:30)

There's an issue with the selected model (claude-3-5-sonnet-20241022). It may not exist or you may not have access to it. Run /model to pick a different model.

---

## 🧑 用户 (2026-06-04T10:07:42)

curl -X POST "https://api.llm.ustc.edu.cn/v1/chat/completions" -H "Content-Type: application/json" -H "Authorization: Bearer sk-x34NcMYTJflY-0ryg1Xj5A" -d '{"model": "deepseek-v4-flash-ascend", "messages": [{"role": "user", "content": "hello"}]}'

---

## 🤖 Claude (2026-06-04T10:07:42)

There's an issue with the selected model (claude-3-5-sonnet-20241022). It may not exist or you may not have access to it. Run /model to pick a different model.

---

## 🧑 用户 (2026-06-04T10:08:00)

# 1. 清理：去除~/.zshrc中包含ANTHROPIC的所有配置行，避免冲突
sed -i '' '/ANTHROPIC_/d' ~/.zshrc

# 2. 清理：删除当前会话中的环境变量，立刻生效
unset ANTHROPIC_BASE_URL ANTHROPIC_AUTH_TOKEN ANTHROPIC_MODEL \
      ANTHROPIC_DEFAULT_OPUS_MODEL ANTHROPIC_DEFAULT_SONNET_MODEL \
      ANTHROPIC_DEFAULT_HAIKU_MODEL CLAUDE_CODE_SUBAGENT_MODEL \
      CLAUDE_CODE_EFFORT_LEVEL

# 3. 配置：根据DeepSeek官方文档写入新的配置[reference:4][reference:5]
#    这里我们用更合适的deepseek-v4-pro做主力模型，flash做轻量模型[reference:6]
cat >> ~/.zshrc << 'EOF'

# DeepSeek API Configuration for Claude Code
export ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
export ANTHROPIC_AUTH_TOKEN="9378cea3fc553d83c6f47260f6c70fcec1718f6b8850940f4520a8d1da2b2a27"
export ANTHROPIC_MODEL="deepseek-v4-pro"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-pro"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-pro"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_SUBAGENT_MODEL="deepseek-v4-flash"
export CLAUDE_CODE_EFFORT_LEVEL="max"
EOF

# 4. 加载：使配置立即生效
source ~/.zshrc

---

## 🤖 Claude (2026-06-04T10:08:01)

There's an issue with the selected model (claude-3-5-sonnet-20241022). It may not exist or you may not have access to it. Run /model to pick a different model.

---

## 🧑 用户 (2026-06-04T10:08:44)

# 1. 清除所有可能冲突的环境变量，确保从干净状态开始
unset ANTHROPIC_BASE_URL ANTHROPIC_AUTH_TOKEN ANTHROPIC_MODEL \
      ANTHROPIC_DEFAULT_OPUS_MODEL ANTHROPIC_DEFAULT_SONNET_MODEL \
      ANTHROPIC_DEFAULT_HAIKU_MODEL CLAUDE_CODE_SUBAGENT_MODEL \
      CLAUDE_CODE_EFFORT_LEVEL

# 2. 写入科大的精准配置
#    科大API端点: https://api.llm.ustc.edu.cn/v1
#    科大API Key: sk-x34NcMYTJflY-0ryg1Xj5A
#    科大模型名: deepseek-v4-flash-ascend
cat >> ~/.zshrc << 'EOF'

# USTC DeepSeek API Configuration for Claude Code
export ANTHROPIC_BASE_URL="https://api.llm.ustc.edu.cn/v1"
export ANTHROPIC_AUTH_TOKEN="sk-x34NcMYTJflY-0ryg1Xj5A"
export ANTHROPIC_MODEL="deepseek-v4-flash-ascend"
export ANTHROPIC_DEFAULT_OPUS_MODEL="deepseek-v4-flash-ascend"
export ANTHROPIC_DEFAULT_SONNET_MODEL="deepseek-v4-flash-ascend"
export ANTHROPIC_DEFAULT_HAIKU_MODEL="deepseek-v4-flash-ascend"
export CLAUDE_CODE_SUBAGENT_MODEL="deepseek-v4-flash-ascend"
export CLAUDE_CODE_EFFORT_LEVEL="max"
EOF

# 3. 让新配置立即在当前终端生效
source ~/.zshrc

---

## 🤖 Claude (2026-06-04T10:08:44)

There's an issue with the selected model (claude-3-5-sonnet-20241022). It may not exist or you may not have access to it. Run /model to pick a different model.

---

## 🧑 用户 (2026-06-04T10:18:30)

# 让 Claude Code 连接到本地代理
export ANTHROPIC_BASE_URL="http://localhost:3456/v1"
export ANTHROPIC_AUTH_TOKEN="any-placeholder-value"

# 启动 Claude Code
claude

---

## 🤖 Claude (2026-06-04T10:18:30)

There's an issue with the selected model (claude-3-5-sonnet-20241022). It may not exist or you may not have access to it. Run /model to pick a different model.

---
