# 对话摘要 - 2026-06-19 v6.7

## 根因
POST /api/jobs 500: jobs 表缺 candidates 表已有的列：
- industry_tags, role_type, source, source_url, ats_score, skills, department, employment_type, urgency
- save_job 的 INSERT 缩进在 if role_type 块里（非主分支走不到）

## 修复
1. ALTER TABLE jobs ADD 缺的全部列
2. save_job 的 valid_cols 补上 source, source_url, industry_tags, role_type
3. INSERT 从 if role_type 块提出来（加 "if not existing" 守卫）
4. deploy-safety skill 加 DB 迁移规则

## 验证
- POST /api/jobs → {"id":"...","status":"ok"} ✅
- POST /api/fast-match → 10 matches, 0 errors ✅
- health: {"status":"ok"} ✅
- frontend: 200 ✅
- emoji: 10/10 clean ✅
- Git: 26b0c70 ✅
