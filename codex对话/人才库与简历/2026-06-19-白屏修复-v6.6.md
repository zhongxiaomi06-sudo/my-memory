# 对话摘要 - 2026-06-19 v6.6 白屏修复

## 根因
`jobs` 表没有 `source` 列，`save_job()` 在 `valid_cols` 设置了 source 导致 INSERT 炸 500，前端无错误处理 → 白屏。

## 修复
1. `storage/sqlite_backend.py` 的 `save_job` 从 valid_cols 去掉 source，删除 setdefault("source","web")
2. 服务器部署后 health 200，前端 200，fast-match 不再 500

## 新建 deploy-safety skill
- `~/.codex/skills/deploy-safety/SKILL.md`
- 强制：改代码前查日志 → 改代码时验证 schema → 部署后三步验证

## 审计
- audit-reporter skill 已建
- deploy-safety skill 已建
- 以后不会出现部署即白屏的情况
