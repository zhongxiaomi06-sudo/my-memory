# 对话摘要 - 2026-06-19 白屏防护系统

## 根因
上次白屏原因是 jobs 表无 source 列导致 save_job INSERT 炸 500，前端无错误处理。

## 本次修复
1. 修复 source 列问题（v6.6 已修）
2. 创建 whitescreen_guard.py — 6项自动检查
3. 更新 deploy-safety skill — 部署流程强制白屏检查
4. 后端日志无 500 ✅ 前端 HTML 200 ✅ 所有 API 正常 ✅

## 白屏防护清单
- HTML 200 + root div
- /health ok
- /api/stats 返回 JSON
- /api/candidates 返回列表
- /api/jobs 返回列表
- JS asset 可加载

以后每次部署后跑 guard/whitescreen_guard.py，任一失败不可交付。
