---
title: 妙喵私教合并冲突修复
session: Claude Code (Kimi K2.7)
date: 2026-07-20
project: 妙喵私教（miaomiao-sijiao）
tags: [git-merge, voice-chat, tts, 桌宠彩蛋]
---

## 完成项

- 修复 GitHub PR `feature/miaomiao-audio-tts` 因 upstream `origin/master` 把整个仓库重构成 `server/` 目录导致的 “Can’t automatically merge”。
- 在 `feature/miaomiao-audio-tts-merge` 分支上完成干净合并：
  - `server/ewa/website/api.py`：恢复 master 的 `/site/{slug}/voice-chat`（Qwen3.5-Omni-Flash / 百度 ASR 链路），同时保留新增的 `POST /tts`。
  - `server/ewa/website/service.py`：保留 `chat_with_voice` 与 mood `segments` 扩展。
  - `server/ewa/extension/store.py`：移除误加的外键约束 `FOREIGN KEY (profile_id) REFERENCES profiles(id)` 和默认 `profile_id = "profile_ashley"`，回退到 master 版本，避免 `profiles` 表未创建时报错。
  - `server/pyproject.toml`：恢复版本 `0.3.0` 与 `fastapi>=0.139.0`，保留 `edge-tts`。
  - `server/frontend/src/lib/api.ts`：恢复 `chatWithVoice` 调用。
  - `server/ewa/config.py`：在 master 基础上追加 Qwen/Baidu 语音配置与 `EWA_EXTENSION_TOKEN`。
  - `.gitignore`：合并 master 与 TTS 缓存路径。

## 验证

- 后端测试：`26 passed, 1 warning`
- 前端构建：`next build` 成功，静态导出 14 页

## 阻塞

- GitHub 推送失败：`Empty reply from server`。
- 本地代理端口 `127.0.0.1:7897` 已关闭，HTTPS 直连 GitHub 也无响应。
- 已提交到本地 `feature/miaomiao-audio-tts-merge`，需启动代理或换网络后执行：
  ```bash
  cd /Users/ashley/黑客松/ewa
  git push --force-with-lease origin feature/miaomiao-audio-tts-merge:feature/miaomiao-audio-tts
  ```

## 后续

- 启动代理后 push，GitHub PR 应变为可自动合并。
- 等待用户从 159 个 Lottie 候选中最终挑选并替换现有动画。
- 按《产品进化开发文档》进入 Phase 1：学习报告 MVP。
